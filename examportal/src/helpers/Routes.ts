// all the main routes in the application are to be used as enum

export enum RoutesEnum {
    // routes in permissions
    DEFAULT = '/',
    LOGIN = 'login',
    LANDING = 'landing',
    SIGNUP = 'signup',
    CONGRATS = 'congrats',
      
    // app.tsx routes
    NOTFOUND = 'notFound' 
  }